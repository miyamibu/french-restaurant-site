# Command Table

| Step | Goal | Risk Class | Command | Expected Success Signal | Rollback / Safety Net |
|---|---|---|---|---|---|
| 1 | Set canonical paths | `READ_ONLY` | `ROOT="/Users/mimac/Desktop/bistro-reservation"; DESKTOP="/Users/mimac/Desktop"; TS="$(date +%Y%m%d-%H%M%S)"; BACKUP="$DESKTOP/bistro-reservation.backup-$TS"; CLONE="$DESKTOP/french-restaurant-site.recovered-$TS"; REMOTE="https://github.com/miyamibu/french-restaurant-site.git"` | Variables expand without error | Re-open shell and reset variables |
| 2 | Snapshot current folder | `SAFE_LOCAL_MUTATION` | `ditto "$ROOT" "$BACKUP"` | `test -f "$BACKUP/package.json"` passes | Keep using original `"$ROOT"` if later steps fail |
| 3 | Create clean git-backed clone | `REMOTE_OR_STATEFUL_MUTATION` | `git clone "$REMOTE" "$CLONE"` | `test -d "$CLONE/.git"` passes | Delete only the failed clone folder, keep `"$ROOT"` and `"$BACKUP"` |
| 4 | Verify clean clone remote | `READ_ONLY` | `git -C "$CLONE" remote -v` | Shows `origin https://github.com/miyamibu/french-restaurant-site.git` | None needed |
| 5 | Verify remote branches | `READ_ONLY` | `git -C "$CLONE" branch -r` | Includes `origin/main` and `origin/codex/rescue-cleanup` | Stop if branches are missing |
| 6 | Confirm app subtree exists | `READ_ONLY` | `APP_CLONE="$CLONE/bistro-reservation"; test -f "$APP_CLONE/package.json" && test -d "$APP_CLONE/src" && test -f "$APP_CLONE/prisma/schema.prisma"` | Shell exits `0` | Stop if repo layout is different |
| 7 | Diff old folder vs clean clone | `READ_ONLY` | `diff -rq "$ROOT" "$APP_CLONE" -x node_modules -x .next -x .vercel -x '*.log' -x '*.png' -x '*.jpg' -x '*.jpeg' -x '*.webp' -x tsconfig.tsbuildinfo | tee "$DESKTOP/bistro-recovery-diff-$TS.txt"` | Diff report written for review | Keep both copies intact; do not copy files yet |
| 8 | Align Node to Vercel 24.x | `SAFE_LOCAL_MUTATION` | `brew install node@24 && export PATH="/opt/homebrew/opt/node@24/bin:$PATH" && hash -r && node -v` | `node -v` starts with `v24.` | Remove PATH export or uninstall `node@24` later if desired |
| 9 | Restore deterministic deps | `SAFE_LOCAL_MUTATION` | `cd "$APP_CLONE" && npm ci` | Install completes cleanly | Delete `node_modules` and rerun after fixing Node version |
| 10 | Reuse verified Vercel project link | `SAFE_LOCAL_MUTATION` | `cd "$APP_CLONE" && mkdir -p .vercel && cp "$ROOT/.vercel/project.json" .vercel/project.json && cat .vercel/project.json` | JSON shows project `bistro-centquatre-104` | Remove `.vercel/` if needed |
| 11 | Backup env files before pull | `SAFE_LOCAL_MUTATION` | `cd "$APP_CLONE" && TS_ENV="$(date +%Y%m%d-%H%M%S)" && [ -f .env ] && cp .env ".env.backup-$TS_ENV"; [ -f .env.local ] && cp .env.local ".env.local.backup-$TS_ENV"; ls -1 .env* 2>/dev/null || true` | Backup files exist if originals existed | Restore from `.env*.backup-*` |
| 12 | Pull Vercel env locally | `REMOTE_OR_STATEFUL_MUTATION` | `cd "$APP_CLONE" && npx vercel env pull .env.local` | CLI confirms env pull completed | Restore `.env.local` from backup |
| 13 | Verify required env presence | `READ_ONLY` | `cd "$APP_CLONE" && node -e "const fs=require('fs');const need=['DATABASE_URL','BASE_URL','ADMIN_BASIC_USER','ADMIN_BASIC_PASS','NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','CRON_SECRET'];const have=new Set();for(const f of ['.env','.env.local']){if(!fs.existsSync(f))continue;for(const line of fs.readFileSync(f,'utf8').split(/\\r?\\n/)){if(!line||/^\\s*#/.test(line))continue;const i=line.indexOf('=');if(i>0)have.add(line.slice(0,i));}}console.log(JSON.stringify({present:need.filter(k=>have.has(k)),missing:need.filter(k=>!have.has(k))},null,2));"` | `missing` is empty or at least no `DATABASE_URL` missing | Stop and restore backup if pulled env looks wrong |
| 14 | Read-only Prisma check | `READ_ONLY` | `cd "$APP_CLONE" && npx prisma validate && npx prisma migrate status` | Prisma validates and status identifies target | Stop if target DB is unknown |
| 15 | Decision gate for migrations | `IRREVERSIBLE_OR_RISKY` | `cd "$APP_CLONE" && npx prisma migrate deploy` | Only run after target DB is confirmed intentional | Do not run on unknown DB; rollback depends on DB backup strategy |
| 16 | Production-like local build | `SAFE_LOCAL_MUTATION` | `cd "$APP_CLONE" && npm run build` | Build completes successfully | Inspect env and Prisma gate if it fails |
| 17 | Local smoke test | `SAFE_LOCAL_MUTATION` | `cd "$APP_CLONE" && npm run dev` | Server prints `Ready` | Ctrl-C to stop |
| 18 | Verify local HTTP response | `READ_ONLY` | `curl -I http://localhost:3000/ && curl -s http://localhost:3000/ | head -n 8` | `HTTP/1.1 200 OK` and HTML output | Stop server with Ctrl-C |
